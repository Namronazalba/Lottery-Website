<%= render "layouts/navbar_admin/navadmin" %> <br>
<div class="main">
  <div class="border p-3">
    <%= form_tag({ url: admin_winners_path }, { method: :get }) do %>
      <div>
        <%= label_tag :find_by %>
      </div>

      <div class="d-inline p-2">
        <%= label_tag :serial_number %>
        <%= text_field_tag :serial_number, '' %>
      </div>

      <div class="d-inline p-2">
        <%= label_tag :email %>
        <%= text_field_tag :email, '' %>
      </div>

      <div class="d-inline p-2">
        <%= label_tag :state %>
        <%= text_field_tag :state, '' %>
      </div>
      <br> <br>

      <div>
        <%= label_tag :date_created %>
      </div>

      <div class="d-inline p-2">
        <%= label_tag :start_date %>
        <%= datetime_field_tag :start_date, '' %>
      </div>

      <div class="d-inline p-2">
        <%= label_tag :end_date %>
        <%= datetime_field_tag :end_date, '' %>
      </div>

      <div>
        <%= submit_tag :Search, class:"btn btn-primary" %>
      </div>
    <% end %>
  </div>

  <table class="table table-striped border border-success">
    <h2 scope="col">All Bets</h2>
    <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Serial number</th>
      <th scope="col">Email</th>
      <th scope="col">State</th>
      <th scope="col">Batch count</th>
      <th scope="col">Created at</th>
      <th scope="col">Action</th>
    </tr>
    </thead>
    <% @winners.each do |win| %>
      <tbody>
      <tr>
        <th scope="row"><%= win.id %></th>
        <td><%= win.serial_number %></td>
        <td><%= win.user.email %></td>
        <td><%= win.state %></td>
        <td><%= win.item_batch_count %></td>
        <td><%= win.created_at %></td>
        <td>
          <%= button_to "Submit", admin_winner_submit_path(win), class: "btn btn-primary", style:"width: 80px;", method: :put if win.may_submit? %>
          <%= button_to "Pay",  admin_winner_pay_path(win),class: "btn btn-primary", style:"width: 80px;",method: :put if win.may_pay? %>
          <%= button_to "Ship",  admin_winner_ship_path(win),class: "btn btn-primary", style:"width: 80px;",method: :put if win.may_ship? %>
          <%= button_to "Deliver",  admin_winner_deliver_path(win),class: "btn btn-primary", style:"width: 80px;",method: :put if win.may_deliver? %>
          <%= button_to "Publish",  admin_winner_publish_path(win),class: "btn btn-primary", style:"width: 80px;",method: :put if win.may_publish? %>
          <%= button_to "Remove publish",  admin_winner_ramove_publish_path(win),class: "btn btn-primary", style:"width: 150px;",method: :put if win.may_remove_publish? %>
        </td>
      </tr>
      </tbody>
    <% end %>
  </table>
</div>